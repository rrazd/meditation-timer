// src/scenes/rain.ts — Stub. Full implementation in Plan 02.
import type { IScene, SceneOptions } from './scene.interface.js';
export class RainScene implements IScene {
  init(_canvas: HTMLCanvasElement, _options: SceneOptions): void {}
  update(_dt: number, _progress: number): void {}
  resize(_width: number, _height: number): void {}
  destroy(): void {}
}
